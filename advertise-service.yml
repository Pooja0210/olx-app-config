spring:
  datasource:
    url: jdbc:mysql://localhost:3306/olx-advertises
    username: root
    password: root123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate :
        dialect: org.hibernate.dialect.MySQL8Dialect
